// Package api exposes the HTTP layer: generated chi-server bindings with 501
// stubs, endpoint overrides in handlers_*.go, SPA static serving and
// localhost middleware. It depends on app/domain only; assembly happens in
// cmd.
package api
